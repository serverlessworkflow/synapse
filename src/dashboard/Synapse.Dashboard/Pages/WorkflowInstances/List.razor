@page "/workflow-instances"
@using Neuroglia.Data
@using Synapse.Integration.Models
@using Synapse.Integration.Services
@inject IServiceProvider ServiceProvider

<PageTitle>Workflow Instances</PageTitle>
<AppHeader>
    <span class="h5 m-0 p-0">Workflow instances</span>
</AppHeader>

<DataTable DataSource="new ODataQueryable<V1WorkflowInstanceDto>(this.ServiceProvider)" AutoGenerateColumns="true" OrderByDefault="(IQueryable query) => ((IQueryable<V1WorkflowInstanceDto>)query).OrderByDescending(c => c.CreatedAt)" />

@code {

   

}
